import AskPage from '@/sections/pages/ask-page'

export const metadata = {
  title: 'Ask | Northwest Oregon PAC',
  description:
    'Ask Northwest Oregon PAC a question. Send us your policy question, share a candidate to endorse, or raise an issue in your neighborhood — we read every message.',
}

export default function Page() {
  return <AskPage />
}
